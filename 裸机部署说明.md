# 电力分析能力评测数据采集与标注系统裸机部署说明

本文说明如何在不使用 Docker 的情况下，将系统部署到 Linux 服务器。

## 1. 运行环境

推荐环境：

- Ubuntu 20.04 / 22.04 / 24.04，或其他带 systemd 的 Linux 发行版
- Python 3.10 及以上
- Git
- ffmpeg
- systemd
- 可访问 Qwen 多模态模型接口的网络环境

Python 依赖见 `requirements.txt`：

- `fastapi`
- `uvicorn[standard]`
- `python-multipart`
- `redis`
- `requests`

说明：

- 裸机部署默认使用 SQLite 队列和 SQLite 数据库，不强制依赖 Redis。
- Docker 部署可以使用 Redis；裸机部署为了简单可靠，默认不启用 Redis。
- 默认管理员账户：`admin`
- 默认管理员密码不在公开文档中展示；部署后请按内部流程获取或修改。
- 默认外观：简洁外观

## 2. 一键裸机安装部署

在全新服务器上执行：

```bash
curl -fsSL https://raw.githubusercontent.com/ArchyYu-noHangUp/vl-data-gen/main/scripts/install_bare_metal.sh | sudo bash
```

如果服务器不能访问 GitHub，可以先把项目拷贝到服务器，然后在项目根目录执行：

```bash
sudo bash scripts/install_bare_metal.sh
```

默认安装目录：

```text
/opt/vl-data-gen/source
```

默认访问地址：

```text
http://服务器IP:8000
```

## 3. 一键脚本可配置参数

可以通过环境变量修改安装目录、端口和并发。

示例：

```bash
sudo APP_ROOT=/data/vl-data-gen \
  PORT=8000 \
  WEB_WORKERS=8 \
  WORKER_CONCURRENCY=12 \
  bash scripts/install_bare_metal.sh
```

参数说明：

- `APP_ROOT`：部署根目录，默认 `/opt/vl-data-gen`
- `SOURCE_DIR`：源码目录，默认 `$APP_ROOT/source`
- `PORT`：Web 端口，默认 `8000`
- `WEB_WORKERS`：Web 进程数，默认 `4`
- `WORKER_CONCURRENCY`：后台任务 worker 数，默认 `4`
- `REPO_URL`：Git 仓库地址
- `BRANCH`：Git 分支，默认 `main`

针对 64 核、500G 内存、Qwen 算力充足的内网服务器，可以设置：

```bash
sudo WEB_WORKERS=8 WORKER_CONCURRENCY=16 bash scripts/install_bare_metal.sh
```

## 4. 手动裸机部署步骤

### 4.1 安装系统依赖

Ubuntu/Debian：

```bash
sudo apt-get update
sudo apt-get install -y python3 python3-venv python3-pip git ffmpeg ca-certificates
```

### 4.2 获取代码

```bash
sudo mkdir -p /opt/vl-data-gen
sudo git clone https://github.com/ArchyYu-noHangUp/vl-data-gen.git /opt/vl-data-gen/source
cd /opt/vl-data-gen/source
```

### 4.3 创建虚拟环境并安装 Python 依赖

```bash
python3 -m venv .venv
.venv/bin/python -m pip install --upgrade pip
.venv/bin/pip install -r requirements.txt
```

### 4.4 创建运行目录

```bash
mkdir -p data runs logs sample_dataset
```

目录说明：

- `data/`：SQLite 数据库
- `runs/`：上传图片、中间结果、校核结果
- `logs/`：日志
- `sample_dataset/`：管理员保存后的样本集

### 4.5 启动服务

临时启动：

```bash
.venv/bin/uvicorn vl_app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

另开终端启动 worker：

```bash
.venv/bin/python -m vl_app.worker
```

生产环境推荐使用一键脚本生成 systemd 服务。

## 5. systemd 服务管理

一键脚本会创建以下服务：

- `vl-data-gen-web.service`
- `vl-data-gen-worker@1.service`
- `vl-data-gen-worker@2.service`
- ...

查看状态：

```bash
sudo systemctl status vl-data-gen-web
sudo systemctl status 'vl-data-gen-worker@*'
```

查看日志：

```bash
sudo journalctl -u vl-data-gen-web -f
sudo journalctl -u 'vl-data-gen-worker@*' -f
```

重启：

```bash
sudo systemctl restart vl-data-gen-web
sudo systemctl restart 'vl-data-gen-worker@*'
```

停止：

```bash
sudo systemctl stop vl-data-gen-web
sudo systemctl stop 'vl-data-gen-worker@*'
```

## 6. 升级代码

进入源码目录：

```bash
cd /opt/vl-data-gen/source
sudo git pull
sudo .venv/bin/pip install -r requirements.txt
sudo systemctl restart vl-data-gen-web
sudo systemctl restart 'vl-data-gen-worker@*'
```

如果使用一键脚本部署，也可以再次执行：

```bash
sudo bash scripts/install_bare_metal.sh
```

脚本会拉取最新代码、安装依赖并重启服务。

## 7. 常见问题

### 7.1 8000 端口被占用

查看占用：

```bash
sudo ss -ltnp 'sport = :8000'
```

换端口部署：

```bash
sudo PORT=8080 bash scripts/install_bare_metal.sh
```

### 7.2 模型调用失败

检查数据处理页的模型配置：

- URL 是否正确
- Model Name 是否正确
- API Key 是否填写
- 服务器是否能访问模型服务地址

### 7.3 上传或裁图失败

确认 `ffmpeg` 已安装：

```bash
ffmpeg -version
```

确认运行目录可写：

```bash
ls -ld data runs logs sample_dataset
```

## 8. Docker 部署

Docker 部署说明见：

```text
docker_release/docker启动说明.md
```

Docker 镜像文件不建议提交到 GitHub。当前本机生成的镜像位于：

```text
docker_release/vl-data-gen-0.3.0.tar.gz
```
