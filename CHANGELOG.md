# 更新日志

## 0.4.4 - 2026-06-12

本版本扩展了原始材料格式，并重构问题与答案的顺序匹配和校核体验。

- 数据处理页的问题与答案均支持上传图片或 PDF；PDF 按 220 DPI 分页渲染后进入多模态处理流程。
- 题目编号改为按问题提取顺序生成，不再直接采用原始材料题号。
- 问题和答案分别提取后，采用原题号优先、剩余内容按顺序补配的方式完成匹配，兼容不同题型分别编号的材料。
- 题目类型新增“填空题”，并同步更新模型判断提示词。
- 查看结果页新增“删除问题”功能，删除后自动重新生成连续题目编号。
- 答案中的 LaTeX 公式统一补充 `$...$` 或 `$$...$$` 定界符，旧结果打开时也会自动规范化，确保公式预览正确渲染。
- 新增 PyMuPDF 依赖，兼容 Linux、Docker 和 Windows Server 裸机环境的 PDF 渲染。
- 版本号更新为 `0.4.4`。

## 0.4.3 - 2026-06-01

本版本聚焦 Docker 和内网部署下的公式预览可靠性。

- 新增本地公式渲染兜底脚本，避免内网环境无法访问 MathJax CDN 时公式预览直接显示 LaTeX。
- 查看结果、样本列表、样本审核页面均接入本地公式渲染兜底。
- 本地兜底支持常见上下标、分式、根号、希腊字母、角度、点号和带圈符号展示。
- 版本号更新为 `0.4.3`。

## 0.4.2 - 2026-05-28

本版本聚焦系统首页身份信息展示和管理员样本列表详情查看。

- 数据处理页在标准外观和简洁外观下均显示版本号、用户名和欢迎词。
- 管理员样本列表页的题目摘要支持点击查看完整题目。
- 完整题目浮窗展示问题、题图和答案，并对 LaTeX 公式进行渲染预览。
- 版本号更新为 `0.4.2`。

## 0.4.1 - 2026-05-27

本版本聚焦管理员样本列表页面的简化和单题下载体验。

- 样本列表页题目列只展示问题文本前 30 个字符，超出部分以省略号展示。
- 样本列表页不再展示题图和答案内容，减少页面渲染负担并提升整洁度。
- 样本列表页“备注”列调整为“下载”，支持逐题下载单个样本 zip。
- 已审核样本在列表页不再可点击修改，并增加后端保护，避免已审核样本被继续保存修改。
- 版本号更新为 `0.4.1`。

## 0.4.0 - 2026-05-20

本版本重构样本审核流程，聚焦账户任务恢复、样本直接入库和管理员审核体验。

- 用户完成校核后样本直接写入样本集，样本保存层级调整为“数据来源 / 样本名称”。
- 查看结果页“完成校核”调整为“完成并关闭”，完成后关闭页面并重置数据处理页状态。
- 数据处理页增加账户级“重置”能力，可清空当前账户缓存和中间任务状态。
- 账号退出后后台任务继续处理，再次登录可恢复进行中任务或进入已完成结果。
- 管理员系统管理页新增“样本数据状态”，按数据来源展示待审核和已审核数量，并支持下载已审核样本。
- 新增“样本列表”和“样本审核”页面，支持逐条预览、审核、放弃、备注编辑和样本信息下载。
- 样本信息 CSV 对题号做 Excel 兼容处理，避免 `13-1` 被自动识别为日期。
- Windows Server 分支同步 0.4.0 功能，修复临时目录路径和 ffmpeg/ffprobe 查找方式，安装脚本可自动准备本地 ffmpeg。
- 版本号更新为 `0.4.0`。

## 0.3.0 - 2026-05-17

本版本聚焦管理员统一配置、系统管理页结构整理和数据处理前置提示。

- 系统管理页按账户管理、多模态大模型配置、样本集路径配置、数据集管理、样本集状态、外观管理、注册设置、修改建议的顺序展示。
- “账户信息”调整为“账户管理”，“数据管理”调整为“数据集管理”。
- 多模态大模型配置由管理员统一维护，配置后对所有账户生效，并持久保存到下次修改。
- 数据处理页删除用户侧模型配置入口，未配置有效模型时在标题下方高亮提示“请联系管理员配置数据处理模型。”。
- 查看结果页、管理员查看校核结果页延续无下载入口设计，下载校核数据只保留在管理员数据集管理操作列。
- 版本号更新为 `0.3.0`。

## 0.2.3 - 2026-05-17

本版本聚焦管理员集中配置、登录注册体验、外观一致性和校核页可编辑能力。

- 多模态大模型 URL、模型名、API Key 改为只能由管理员在系统管理页配置。
- 数据处理页删除多模态大模型配置入口，普通用户不再接触模型配置和 API Key。
- 系统管理页新增样本集保存路径配置，支持将当前样本集移动到新的绝对路径。
- 查看结果页删除“下载zip”按钮，完成校核时不再提示下载。
- 数据管理操作列新增“下载zip”按钮，下载校核后数据的权限收敛到管理员。
- 注册页接入全局外观配置：默认简洁外观，只有管理员配置为标准外观后才显示标准外观。
- 欢迎页和注册页由后端按当前外观直接返回 HTML，避免刷新时先闪现标准外观。
- 登录和注册表单禁用浏览器历史输入提示，点击输入框时不再自动展示之前填写的用户名、密码、注册码。
- 查看结果页的“题目编号”改为可编辑字段，点击“保存校核”后写回结果。
- 查看结果页问题文本中的题图引用统一支持转换为 Markdown 图片引用格式，例如 `题图![13-4](13-4.jpg)`。
- 默认外观调整为简洁外观。

## 0.2.2 - 2026-05-15

- 增加标准外观 / 简洁外观配置。
- 增加裸机部署说明和一键裸机部署脚本。
- 增加 Docker 部署说明和一键 Docker 部署脚本。
- 移除公开文档中默认管理员密码的直接展示。

## 0.2.1 - 2026-05-14

- 增加数据处理 ID、完成校核流程和管理员样本管理能力。
- 增加 Docker 镜像构建配置和默认并发参数。
